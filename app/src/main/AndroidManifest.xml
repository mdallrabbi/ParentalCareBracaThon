<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mdallrabbi.parentalcarebracathon">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeScActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MomSelectionActivity" />
        <activity android:name=".Beg_DI" />
        <activity
            android:name=".MainBegActivity"
            android:label="@string/title_activity_main_beg"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Bec_DI" />
        <activity
            android:name=".MainBecActivity"
            android:label="@string/title_activity_main_bec"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>